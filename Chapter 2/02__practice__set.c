#include <stdio.h>

int main()
{
    // int a; b=a;
    // int v = 3^3;
    // char dt = '2';
    // float b= (3.0/8-2);
    // printf("%f\n", b);
    // int num;
    // printf("Enter the number\n");
    // scanf("%d", &num);
    // printf("Divisibility test returns: %d", num%97);

    // int x = 2, y = 3, z = 3, k = 1;
    // int result = 3 * x / y - z + k;
    // 3 * 2 / 3 - 3 + 1
    // 6 / 3 - 3 + 1
    // 2 - 3 + 1
    // -1 + 1
    // 0
    // printf("The value pf result is %d\n", result);
    // printf("The value of 3.0 + 1 will be %d\n", 3.0 + 1); 
    return 0;
}